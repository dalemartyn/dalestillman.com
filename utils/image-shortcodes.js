import { renderImage } from './images.js';

export const image              = f => renderImage(f, "image.regular.html");
export const work_main_image    = f => renderImage(f, "image.work-image.html");
export const main_image         = f => renderImage(f, "image.main-image.html");
export const card_image         = f => renderImage(f, "image.card-image.html");
export const instagram_image    = f => renderImage(f, "image.instagram-image.html");
